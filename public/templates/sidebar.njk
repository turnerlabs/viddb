{% block content %}
    <ul class="sidebar-nav">
        <li class="sidebar-brand">
            <a href="#">
                Video-fi
            </a>
        </li>
       {% for v in vids %}
        <li><a href="#" id="{{v.vid}}" class="video">{{v.vid}}</a></li>
        {% endfor %}
    </ul>
{% endblock %}